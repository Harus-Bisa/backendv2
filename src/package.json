{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "start": "cross-env NODE_ENV=production node server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js",
    "prod-forever": "cross-env NODE_ENV=production pm2 start server.js --name prod",
    "dev-forever": "cross-env NODE_ENV=development pm2 start server.js --name dev",
    "prod-forever-restart": "cross-env NODE_ENV=production pm2 restart prod",
    "dev-forever-restart": "cross-env NODE_ENV=development pm2 restart dev",
    "prod-forever-stop": "pm2 stop prod",
    "dev-forever-stop": "pm2 stop dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.8.1"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "jest": "^24.9.0",
    "nodemon": "^2.0.2",
    "prettier": "^1.19.1",
    "supertest": "^4.0.2"
  }
}